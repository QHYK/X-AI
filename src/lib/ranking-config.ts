import { DEFAULT_STAGE4_EVENT_LIMIT } from "../processing/stage4-config.js";

/** @deprecated Event cutoff is owned by Stage 4; retained for Review compatibility. */
export const EVENT_DISPLAY_CUTOFF = DEFAULT_STAGE4_EVENT_LIMIT;
export const LONG_FORM_DISPLAY_CUTOFF = 10;
